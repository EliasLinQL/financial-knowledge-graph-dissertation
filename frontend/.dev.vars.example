# Copy this file to .dev.vars for local AI-generated explanations.
# The dashboard works in deterministic preview mode when no key is configured.
OPENAI_API_KEY=replace_with_your_openai_api_key
OPENAI_MODEL=gpt-4.1-mini
